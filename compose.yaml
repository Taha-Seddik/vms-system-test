name: vms-assessment

x-camera-generator: &camera-generator
  image: vms-camera-generator:local
  build:
    context: ./infra/camera-generator
  restart: unless-stopped
  depends_on:
    mediamtx:
      condition: service_healthy
  healthcheck:
    test: ["CMD-SHELL", "pidof ffmpeg >/dev/null || exit 1"]
    interval: 10s
    timeout: 3s
    start_period: 15s
    retries: 6
  networks:
    - vms

services:
  postgres:
    image: postgres:17.5-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-vms}
      POSTGRES_USER: ${POSTGRES_USER:-vms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vms_dev_password}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 5s
      timeout: 5s
      retries: 12
    networks:
      - vms

  mediamtx:
    build:
      context: ./infra/mediamtx
    image: vms-mediamtx:local
    restart: unless-stopped
    command: /mediamtx.yml
    ports:
      - "127.0.0.1:${HLS_PORT:-8888}:8888"
    environment:
      MTX_HLSALLOWORIGINS: >-
        http://localhost:${FRONTEND_PORT:-3000},http://localhost:5173
    volumes:
      - ./infra/mediamtx/mediamtx.yml:/mediamtx.yml:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -q -O /dev/null http://127.0.0.1:9997/v3/paths/list || exit 1",
        ]
      interval: 5s
      timeout: 3s
      start_period: 5s
      retries: 12
    networks:
      - vms

  camera-1:
    <<: *camera-generator
    environment:
      CAMERA_PATH: camera-1
      CAMERA_LABEL: Camera 1 - Entrance
      CAMERA_HUE: 0

  camera-2:
    <<: *camera-generator
    environment:
      CAMERA_PATH: camera-2
      CAMERA_LABEL: Camera 2 - Loading Bay
      CAMERA_HUE: 70

  camera-3:
    <<: *camera-generator
    environment:
      CAMERA_PATH: camera-3
      CAMERA_LABEL: Camera 3 - Parking
      CAMERA_HUE: 150

  camera-4:
    <<: *camera-generator
    environment:
      CAMERA_PATH: camera-4
      CAMERA_LABEL: Camera 4 - Warehouse
      CAMERA_HUE: 230

  api:
    image: vms-api:local
    build:
      context: ./backend
      dockerfile: Vms.Api/Dockerfile
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__VmsDatabase: >-
        Host=postgres;Port=5432;Database=${POSTGRES_DB:-vms};
        Username=${POSTGRES_USER:-vms};Password=${POSTGRES_PASSWORD:-vms_dev_password}
      Jwt__Issuer: ${JWT_ISSUER:-vms-api}
      Jwt__Audience: ${JWT_AUDIENCE:-vms-web}
      Jwt__SigningKey: ${JWT_SIGNING_KEY:-development-only-signing-key-change-before-production-2026}
      Cors__AllowedOrigins__0: http://localhost:${FRONTEND_PORT:-3000}
      CameraMonitoring__IntervalSeconds: ${CAMERA_MONITOR_INTERVAL_SECONDS:-15}
      CameraMonitoring__ProbeTimeoutSeconds: ${CAMERA_PROBE_TIMEOUT_SECONDS:-5}
      RecordingStorage__Path: /var/lib/vms/recordings
      RecordingStorage__WarningPercent: ${STORAGE_WARNING_PERCENT:-80}
      RecordingStorage__CriticalPercent: ${STORAGE_CRITICAL_PERCENT:-90}
      Recording__ContinuousSegmentSeconds: ${CONTINUOUS_SEGMENT_SECONDS:-10}
      Recording__EventDurationSeconds: ${EVENT_RECORDING_SECONDS:-8}
      Recording__MinimumCaptureSeconds: ${MINIMUM_CAPTURE_SECONDS:-6}
      Recording__KeyframeIntervalSeconds: ${KEYFRAME_INTERVAL_SECONDS:-30}
    ports:
      - "127.0.0.1:${API_PORT:-8080}:8080"
    volumes:
      - recording-data:/var/lib/vms/recordings
    depends_on:
      postgres:
        condition: service_healthy
      mediamtx:
        condition: service_healthy
      camera-1:
        condition: service_healthy
      camera-2:
        condition: service_healthy
      camera-3:
        condition: service_healthy
      camera-4:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "dotnet", "Vms.Api.dll", "--health-check"]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 6
    networks:
      - vms

  frontend:
    image: vms-frontend:local
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
        VITE_HLS_BASE_URL: ${VITE_HLS_BASE_URL:-http://localhost:8888}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${FRONTEND_PORT:-3000}:80"
    depends_on:
      api:
        condition: service_healthy
      mediamtx:
        condition: service_healthy
    networks:
      - vms

volumes:
  postgres-data:
  recording-data:

networks:
  vms:
    driver: bridge
